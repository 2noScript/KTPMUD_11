<Window x:Class="h25.Views.t1"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:h25.Views"
        WindowStartupLocation="CenterScreen"
        mc:Ignorable="d"
        
        Title="t1" Height="450" Width="800">
    <!--<DockPanel Margin="10">
        <WrapPanel HorizontalAlignment="Center" DockPanel.Dock="Top" Margin="0,0,0,10">
            <Button Name="btnSaveFile" Click="btnSaveFile_Click">Save file</Button>
        </WrapPanel>
        <TextBox Name="txtEditor" TextWrapping="Wrap"
                 AcceptsReturn="True"
                 ScrollViewer.VerticalScrollBarVisibility="Auto" />
    </DockPanel>-->
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <StackPanel>
            <Button Content="open" Click="Button_Click"/>
            <Button Content="save" Click="Button_Click_1"/>
            <Button Content="click" Click="Button_Click_2"/>
        </StackPanel>
        <StackPanel Grid.Column="1">
            <Image Height="150" Name="one"/>
            <Image Height="150" Name="two"/>
        </StackPanel>
    </Grid>
</Window>
